# Application Environment Configuration
APP_ENV=development
LOG_LEVEL=INFO
HOST=0.0.0.0
PORT=8000

# Data Provider Configuration
# Supported options: 'mock' (default, runs completely without credentials), 'candidate' (reserved for live retrieval research)
DATA_PROVIDER=mock

# Note: In mock mode (default), no external credentials, cookies, or API keys are required.
